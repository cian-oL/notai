[project]
name = "notai"
version = "0.1.0"
description = "Research & Write Notes"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit~=4.6.0",
    "ruff~=0.15.12",
]

[tool.uv]
exclude-newer = "14 days"

[tool.uv.workspace]
members = ["apps/*"]
